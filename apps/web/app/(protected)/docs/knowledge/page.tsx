import Link from "next/link";

export default function KnowledgeDocsPage() {
  return (
    <section className="knowledge-docs">
      <h1>База знаний: как это работает</h1>
      <p className="knowledge-docs-lead">
        Материалы из подключённых баз попадают в системный промпт ассистента. Модель должна опираться на эти фрагменты и не
        выдумывать факты. Ниже — форматы, лимиты и ограничения.
      </p>

      <h2>Связь с ассистентом</h2>
      <ul>
        <li>
          В разделе <Link href="/assistants">Ассистенты</Link> отметьте одну или несколько баз знаний для ассистента.
        </li>
        <li>
          При каждом сообщении пользователя сервер подбирает ограниченный набор релевантных фрагментов (поиск по тексту и,
          при наличии, по векторам) и передаёт их модели вместе с инструкцией «только факты из материалов».
        </li>
      </ul>

      <h2>Форматы и импорт</h2>
      <ul>
        <li>
          <strong>Файлы:</strong> PDF, DOCX, TXT, MD, JSON и др. текстовые — текст извлекается на сервере, документ режется на
          перекрывающиеся чанки для поиска. Старый .doc, сканы без OCR и бинарные форматы могут не извлекаться — добавьте
          текст вручную.
        </li>
        <li>
          <strong>Текст:</strong> короткие заметки хранятся как одна запись. Длинные (свыше порога в интерфейсе) автоматически
          режутся на чанки, как файлы.
        </li>
        <li>
          <strong>URL:</strong> страница загружается с таймаутом и лимитом размера, HTML очищается от тегов, текст чанкуется.
          Сайты с блокировкой ботов или тяжёлым JS могут не отдать контент.
        </li>
      </ul>

      <h2>Поиск и эмбеддинги</h2>
      <ul>
        <li>
          Быстрый отбор: полнотекстовый индекс (GIN) и ILIKE по словам из вопроса пользователя, плюс «свежие» чанки при
          нехватке совпадений.
        </li>
        <li>
          Семантика: при включённой миграции pgvector и настроенном API эмбеддингов (OpenRouter или OpenAI) чанки получают
          векторы фоновым воркером; в диалоге дополнительно подмешиваются ближайшие по смыслу фрагменты.
        </li>
        <li>
          Воркер: <code>POST /api/knowledge/embeddings/worker</code> с заголовком{" "}
          <code>x-embedding-worker-secret</code> (значение из <code>EMBEDDING_WORKER_SECRET</code>), например по cron раз в
          минуту.
        </li>
      </ul>

      <h2>ИИ-подсказки при наполнении</h2>
      <p>
        Кнопка «ИИ: предложить карточки» вызывает модель для разбиения черновика на заголовок+текст. Результат нужно
        проверить и только затем сохранять — автоматическая запись в базу не выполняется.
      </p>

      <h2>Переменные окружения (сервер)</h2>
      <ul>
        <li>
          <code>EMBEDDING_WORKER_SECRET</code> — секрет для воркера эмбеддингов.
        </li>
        <li>
          <code>EMBEDDING_MODEL</code> — модель эмбеддингов для OpenAI API (по умолчанию <code>text-embedding-3-small</code>
          ).
        </li>
        <li>
          <code>OPENROUTER_EMBEDDING_MODEL</code> — модель эмбеддингов для OpenRouter (по умолчанию{" "}
          <code>openai/text-embedding-3-small</code>).
        </li>
        <li>
          <code>DISABLE_EMBEDDING_JOBS=true</code> — не ставить задачи эмбеддингов при импорте.
        </li>
      </ul>

      <p className="knowledge-docs-back">
        <Link href="/knowledge">← К базе знаний</Link>
      </p>
    </section>
  );
}
