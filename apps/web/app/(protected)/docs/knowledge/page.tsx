import Link from "next/link";

export default function KnowledgeDocsPage() {
  return (
    <section className="knowledge-docs">
      <h1>База знаний: как это работает</h1>
      <p className="knowledge-docs-lead">
        Материалы из подключённых баз попадают в системный промпт ассистента. Модель опирается на эти фрагменты и не выдумывает
        факты. Ниже — три способа создания, форматы, лимиты и все настройки.
      </p>

      <h2>Три способа создать базу</h2>
      <ol>
        <li>
          <strong>Файлы</strong> — перетащите PDF/DOCX/TXT/MD/JSON/CSV (до 6 за раз, до 10 МБ каждый). Сервер извлечёт текст,
          порежет на чанки с перекрытием и подключит к поиску.
        </li>
        <li>
          <strong>Готовый текст</strong> — вставьте готовые фрагменты (заголовок необязателен — подставим автоматически, если
          включена опция «авто-заголовки»). Длинный текст режется на чанки. ИИ-кнопка может разбить черновик на карточки.
        </li>
        <li>
          <strong>URL и sitemap.xml</strong> — одиночная ссылка или пакетный импорт. Укажите список URL (до 40 за запрос) или
          адрес карты сайта — сервер сам распарсит <code>&lt;loc&gt;</code> и загрузит страницы.
        </li>
      </ol>

      <h2>Шаблоны при создании</h2>
      <p>В визарде выбирается один из пресетов — он заполняет параметры чанкинга и режим ответов:</p>
      <ul>
        <li>
          <strong>FAQ</strong> — короткие чанки (1200/150), строгий режим. Для базы вопрос-ответ, скриптов поддержки.
        </li>
        <li>
          <strong>Документация</strong> — 1800/220, строгий режим. Для гайдов и технической справки.
        </li>
        <li>
          <strong>Регламенты/политики</strong> — крупные чанки (2200/260), строгий режим. Для юридических текстов и внутренних
          регламентов.
        </li>
        <li>
          <strong>Маркетинг</strong> — 1500/180, смешанный режим (можно дополнять общими знаниями модели).
        </li>
        <li>
          <strong>Пустая</strong> — значения по умолчанию (1800/200, строго).
        </li>
      </ul>

      <h2>Настройки базы</h2>
      <ul>
        <li>
          <strong>Режим ответов:</strong> <em>строгий</em> — только по базе, ассистент прямо говорит, если информации нет;
          <em> смешанный</em> — ассистент использует базу как основной источник, но может дополнять общими знаниями с явным
          разграничением.
        </li>
        <li>
          <strong>Размер чанка</strong> (300–4000 символов) и <strong>перекрытие</strong> (0–500) — влияют на новые материалы.
          Чтобы пересчитать старые, нажмите «Обновить» в строке записи (TEXT/URL).
        </li>
        <li>
          <strong>Макс. контекст</strong> (2000–40000) — сколько символов чанков передавать модели в одном сообщении. Для дешёвых
          моделей ставьте меньше, для Claude/GPT‑4.x — можно больше.
        </li>
        <li>
          <strong>Авто-заголовки</strong> — если оставили поле пустым при добавлении текста/URL, заголовок подберётся по первой
          осмысленной фразе или пути URL.
        </li>
      </ul>

      <h2>Поиск и эмбеддинги</h2>
      <ul>
        <li>
          Гибридный поиск: полнотекстовый индекс (GIN), <code>ILIKE</code> по словам запроса, «свежие» чанки, семантика через
          <code> pgvector</code>.
        </li>
        <li>
          Семантические векторы считает фоновый воркер (<code>POST /api/knowledge/embeddings/worker</code> с заголовком{" "}
          <code>x-embedding-worker-secret</code>), запускается по cron раз в 1–5 минут.
        </li>
        <li>
          Кнопка <strong>«Пересчитать эмбеддинги»</strong> в заголовке базы ставит в очередь все документы, где есть чанки без
          векторов. Полезно после смены модели эмбеддингов или массового импорта.
        </li>
      </ul>

      <h2>Автоматизация</h2>
      <ul>
        <li>
          <strong>Пакетный импорт URL.</strong> Вставьте список ссылок (по одной на строку) или адрес sitemap — сервер
          пройдёт по ним, очистит HTML, сделает чанки и добавит в базу.
        </li>
        <li>
          <strong>ИИ‑разбивка текста.</strong> Вкладка «Текст» → кнопка «ИИ: разбить на карточки» — модель предложит 2–12
          фрагментов <code>{`{title, content}`}</code>. Вставляете по одному и сохраняете.
        </li>
        <li>
          <strong>Переиндексация записи.</strong> Кнопка «Обновить» в таблице: для URL — перезагружает страницу; для длинного
          текста — пересобирает чанки по текущим настройкам базы.
        </li>
        <li>
          <strong>Идемпотентность.</strong> Файл с тем же содержимым (SHA‑256) заменит предыдущую запись. URL не дублируется при
          пакетном импорте — существующая запись просто переиндексируется.
        </li>
      </ul>

      <h2>Безопасность и ограничения</h2>
      <ul>
        <li>
          URL-импорт запрещает приватные IP и loopback (SSRF-фильтр), таймаут 18 с, лимит 2 МБ на ответ.
        </li>
        <li>
          Сайты с защитой от ботов или сложным JS могут отдать пустой текст — используйте экспорт в файл или готовый текст.
        </li>
        <li>
          Нет OCR: сканы PDF и изображения не распознаются автоматически.
        </li>
      </ul>

      <h2>Переменные окружения (сервер)</h2>
      <ul>
        <li>
          <code>EMBEDDING_WORKER_SECRET</code> — секрет для воркера эмбеддингов.
        </li>
        <li>
          <code>EMBEDDING_MODEL</code> — модель эмбеддингов для OpenAI (по умолчанию <code>text-embedding-3-small</code>).
        </li>
        <li>
          <code>OPENROUTER_EMBEDDING_MODEL</code> — модель эмбеддингов для OpenRouter (по умолчанию{" "}
          <code>openai/text-embedding-3-small</code>).
        </li>
        <li>
          <code>OPENROUTER_SUGGEST_MODEL</code> — модель для ИИ-подсказок карточек (по умолчанию <code>openai/gpt-4o-mini</code>
          ).
        </li>
        <li>
          <code>DISABLE_EMBEDDING_JOBS=true</code> — не ставить задачи эмбеддингов при импорте.
        </li>
      </ul>

      <p className="knowledge-docs-back">
        <Link href="/knowledge">← К базе знаний</Link>
      </p>
    </section>
  );
}
