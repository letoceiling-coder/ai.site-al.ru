DATABASE_URL="postgresql://ai_user:ai_password@localhost:5433/ai_site_al_ru?schema=public"
JWT_SECRET="change-me-in-production"
NODE_ENV="development"
# Self-hosted behind HTTPS: optional override for middleware → /api/auth/refresh (default is loopback when not on Vercel)
# INTERNAL_API_ORIGIN="http://127.0.0.1:3006"

# База знаний: фоновые эмбеддинги (pgvector). Вызов POST /api/knowledge/embeddings/worker с заголовком x-embedding-worker-secret
# EMBEDDING_WORKER_SECRET="change-me"
# EMBEDDING_MODEL="text-embedding-3-small"
# OPENROUTER_EMBEDDING_MODEL="openai/text-embedding-3-small"
# OPENROUTER_SUGGEST_MODEL="openai/gpt-4o-mini"
# DISABLE_EMBEDDING_JOBS="false"
